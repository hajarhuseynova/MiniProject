@model Volume

<div class="container">
	<form method="post" enctype="multipart/form-data">
	<div class="form-group">
		    <label asp-for="@Model.MilliLiters" >Olcu:</label>
			<input type="text" class="form-control" asp-for="@Model.MilliLiters"/>
			<span class="text text-danger" asp-validation-for="@Model.MilliLiters"></span>
	</div>

			@Html.AntiForgeryToken()
	<button type="submit" class="btn btn-info">Yarat</button>
	</form>
</div>